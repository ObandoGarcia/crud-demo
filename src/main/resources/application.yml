spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/db_jpa_crud
    username: postgres
    password: ecuacion
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    show-sql: true
